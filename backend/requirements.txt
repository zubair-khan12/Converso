# --- Web framework (FastAPI) ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32   # form parsing for sqladmin
pydantic-settings==2.14.2

# --- Database / ORM / migrations ---
SQLAlchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10
pgvector==0.3.6

# --- Internal admin panel ---
sqladmin==0.29.0

# --- Auth ---
PyJWT==2.9.0
Werkzeug==3.0.4            # password hashing (generate/check_password_hash) only
cryptography==43.0.3       # encrypting tenant Vapi API keys at rest

# --- Vapi integration ---
httpx==0.27.2               # calling Vapi's REST API
