# --- Web framework (FastAPI) ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32   # form parsing for sqladmin
pydantic-settings==2.14.2

# --- Database / ORM / migrations ---
SQLAlchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10
pgvector==0.3.6

# --- Internal admin panel ---
sqladmin==0.29.0

# --- Auth ---
PyJWT==2.9.0
Werkzeug==3.0.4            # password hashing (generate/check_password_hash) only
cryptography==43.0.3       # encrypting tenant Vapi API keys at rest

# --- Vapi integration ---
httpx==0.27.2               # calling Vapi's REST API

# --- Knowledge base / RAG (LangGraph agent + embeddings) ---
openai==2.48.0                    # embeddings + generation (platform key)
langgraph==1.2.9                  # agentic workflow (KB = tool #1, Cal.com later)
langchain-core==1.5.1
langchain-openai==1.4.1
langchain-text-splitters==1.1.2   # RecursiveCharacterTextSplitter for chunking
pypdf==6.14.2                     # PDF text extraction
